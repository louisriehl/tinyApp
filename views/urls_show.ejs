<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
</head>

<header>
  <% include ./partials/_header %>
</header>

<body style=" display: block; text-align: center;">
  <div class="card" style=" padding-bottom: 20px;">
    <table class="table" align="center" style=" text-align: center; padding-top: 20px; padding-bottom: 20px;">
      <thead class="thead-dark">
        <tr>
        <th>URL Code</th>
        <th>Made On</th>
        <th>Total Visits</th>
        <th>Unique Visitors</th>
        <th>Visitor Statistics</th>
       </tr>
       <tr>
        <td><%= short %></td>
        <td><%= date %></td>
        <td><%= visits %></td>
        <td><%= unique %></td>
        <td><% tracking.forEach(element => {%>
          <% for(let thing in element) { %>
             <p><%= thing %> visited on <br> <%= element[thing]%></p>
              <% } %>
            <% }); %>
        </td>
      </tr>
      </thead>
    </table>
    <form method="POST" action="/urls/<%= short%>?_method=PUT" style=" display: inline-block; margin:auto; width: 50%;">
      <div class="form-group">
      <label for="newLong" style="margin-top: 10px;">Update URL</label>
      <input id="newLong" type="text" name="newLong" class="form-control" style=" width: 100%;">
      <button class="btn btn-info btn-block" type="submit" style="margin-top: 20px;">Update</button>
    </div>
    </form>
    <form action="/urls"><button class="btn btn-outline-warning" type="submit">Cancel</button></form>
  </div>
</body>

</html>